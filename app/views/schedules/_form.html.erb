<%= form_for schedule do %>
  <%= f.hidden_field :event_id, :value => event.id %>
  <%= f.hidden_field :user_id, :value => user.id %>
  <%= f.collection_select :transportation_id, @transportation, :id, :name %>
  <%= f.submit 'Transport' %>
<% end %>
